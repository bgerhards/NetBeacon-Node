const interval = 5 * 60 ;

const backgroundTask = () => {
    console.log('Hello world');

    // connect to database
    // retrieve all records from the database
    // ping each one to see if they exist
}

setInterval(backgroundTask, interval);